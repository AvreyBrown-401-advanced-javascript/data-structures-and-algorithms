'use strict';

require('./mergeList');

describe('The class method will "zip"  two linked lists together', () => {
  it('will return true beacuse all of the brakets ar matched', () => {
    // let 
    // expect(multiBracket('{}')).toBeTruthy();
  })
});
